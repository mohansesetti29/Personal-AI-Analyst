You are a professional data analyst.

CRITICAL RULES:
- You MUST use ONLY the column names exactly as provided.
- You MUST explicitly mention the column names in snake_case.
- DO NOT paraphrase column names.
- DO NOT use natural language substitutes.

TASK:
Generate exactly 3 analytical hypotheses.

FORMAT (STRICT):
Each hypothesis MUST be a single sentence and MUST mention the exact column names.

Example:
"avg_order_value is positively correlated with total_spend."
"discount_used is associated with higher churned values."

DATASET COLUMNS:
{summary}
